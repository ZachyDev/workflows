# name of the workflow
name: Test workflow
on:
  push:
    branches: [ master ]
  pull_request: 
    branches: [ master ]
jobs:
  #job1 
  test:
    
    name: Running the test file
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: trying to check the tests
        run: echo Hello,World!
# job2
  jsdoc:
    name: Generating project documentation
    runs-on: windows-latest
    steps:
      - uses: brunokrauss/jsdoc-action@1
        with:
          jsdoc-path: '.github/workflow/script.js'     
      - name: I am generating a documentation for you
        run: echo process done!

       
    
